{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS"
    },
    "deploy": {
      "startCommand": "python manage.py migrate && python create_superuser.py && gunicorn config.wsgi --log-file -",
      "healthcheckPath": "/admin/",
      "restartPolicyType": "ON_FAILURE"
    }
  }