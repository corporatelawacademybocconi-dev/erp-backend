{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS"
    },
    "deploy": {
      "startCommand": "python manage.py migrate && python manage.py createsuperuser --noinput || true && gunicorn config.wsgi --log-file -",
      "healthcheckPath": "/admin/",
      "restartPolicyType": "ON_FAILURE"
    }
  }